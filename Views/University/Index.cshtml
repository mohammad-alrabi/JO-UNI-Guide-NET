@model JO_UNI_Guide.Helpers.PaginatedList<JO_UNI_Guide.Models.University>

@{
    ViewData["Title"] = "Universities";
    Layout = "_AdminLayout";
}

<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-4">
        <div>
            <h2 class="page-title mb-1">Universities</h2>
            <p class="text-muted mb-0">Manage universities easily</p>
        </div>

        <a asp-action="Create" class="btn btn-main px-4">
            <i class="bi bi-plus-lg me-2"></i> Add University
        </a>
    </div>
    <div class="uni-admin-card shadow-sm p-3 mb-4">
        <form asp-action="Index" method="get">
            <div class="row g-2">
                <div class="col-md-9">
                    <div class="input-group">
                        <span class="input-group-text bg-white border-end-0 text-muted">
                            <i class="bi bi-search"></i>
                        </span>
                        <input type="text"
                               name="searchString"
                               value="@ViewData["CurrentFilter"]"
                               class="form-control border-start-0 shadow-none"
                               placeholder="Search by name or location..." />
                    </div>
                </div>
                <div class="col-md-3 d-flex gap-2">
                    <button class="btn btn-dark w-100">Search</button>
                    <a asp-action="Index" class="btn btn-outline-secondary w-100">Reset</a>
                </div>
            </div>
        </form>
    </div>

    @if (Model.Count == 0)
    {
        <div class="text-center py-5">
            <p class="text-muted mt-3">No universities found match your criteria.</p>
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card uni-admin-card shadow-sm h-100">
                        
                        <div class="uni-logo-container">
                            <img src="@item.Logo"
                                 onerror="this.src='/images/default.png'"
                                 alt="@item.Name Logo" />
                        </div>

                        <div class="card-body-content">
                            <h5 class="uni-name">@item.Name</h5>
                            <p class="uni-location">
                                <i class="bi bi-geo-alt me-1 text-danger"></i> @item.Location
                            </p>
                            
                            <div class="rank-badge mb-3">
                                <i class="bi bi-trophy me-1"></i> QS Rank: @item.Rank_QS
                            </div>

                            <div class="d-flex gap-2 btn-action-group">
                                <a asp-action="Details" asp-route-id="@item.University_ID" class="btn btn-info text-white shadow-none">Details</a>
                                <a asp-action="Edit" asp-route-id="@item.University_ID" class="btn btn-warning text-white shadow-none">Edit</a>
                                <a asp-action="Delete" asp-route-id="@item.University_ID" class="btn btn-danger shadow-none">Delete</a>
                            </div>
                        </div>

                    </div>
                </div>
            }
        </div>

        <div class="d-flex justify-content-center align-items-center gap-3 mt-4">
            <a asp-action="Index"
               asp-route-pageNumber="@(Model.PageIndex - 1)"
               asp-route-currentFilter="@ViewData["CurrentFilter"]"
               class="btn btn-white shadow-sm @(Model.HasPreviousPage ? "" : "disabled text-muted")">
                <i class="bi bi-chevron-left"></i> Previous
            </a>

            <span class="fw-bold text-muted small">
                Page @Model.PageIndex
            </span>

            <a asp-action="Index"
               asp-route-pageNumber="@(Model.PageIndex + 1)"
               asp-route-currentFilter="@ViewData["CurrentFilter"]"
               class="btn btn-white shadow-sm @(Model.HasNextPage ? "" : "disabled text-muted")">
                Next <i class="bi bi-chevron-right"></i>
            </a>
        </div>
    }
</div>