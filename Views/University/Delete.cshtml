@model JO_UNI_Guide.Models.University

<<<<<<< HEAD
<div class="container mt-5 text-center">

    <h2 class="section-title text-danger">Delete University</h2>

    <p>Are you sure you want to delete this?</p>

    <img src="@Model.Logo" class="img-thumbnail mb-3" width="120" />

    <form method="post">
        <input type="hidden" asp-for="University_ID" />

        <button class="btn btn-danger">Delete</button>
        <a href="/University" class="btn btn-secondary">Cancel</a>
    </form>

=======
@{
    ViewData["Title"] = "Delete University";
    Layout = "_AdminLayout"; // <-- التعديل الأهم عشان تفتح جوا الـ Sidebar
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="delete-card shadow-sm border border-danger border-opacity-25">

                <div class="text-center mb-4">
                    <h2 class="page-title text-danger">
                        <i class="bi bi-trash3-fill me-2"></i>Delete University
                    </h2>
                    <p class="text-muted">Are you sure you want to delete this university?</p>
                </div>

                <div class="alert alert-danger text-center fw-bold mb-4">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i> This action cannot be undone!
                </div>

                <div class="text-center mb-4">
                    <img src="@Model.Logo"
                         alt="@Model.Name"
                         class="img-fluid shadow-sm p-1 bg-white border"
                         style="max-width: 120px; border-radius: 15px; object-fit: contain;"
                         onerror="this.src='/images/default.png'" />
                </div>

                <div class="text-center mb-4">
                    <h4 class="fw-bold text-dark mb-1">@Model.Name</h4>
                    <p class="text-muted mb-3"><i class="bi bi-geo-alt-fill text-danger me-1"></i>@Model.Location</p>
                    <span class="badge bg-primary bg-opacity-10 text-primary px-3 py-2 rounded-pill border border-primary">
                        QS Rank: @Model.Rank_QS
                    </span>
                </div>

                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="University_ID" />

                    <div class="d-flex justify-content-center gap-3 flex-wrap mt-2">
                        <button type="submit" class="btn btn-danger px-4 fw-bold shadow-sm">
                            <i class="bi bi-trash-fill me-1"></i> Confirm Delete
                        </button>

                        <a asp-action="Index" class="btn btn-outline-secondary px-4 fw-bold">
                            Cancel
                        </a>
                    </div>
                </form>

            </div>
        </div>
    </div>
>>>>>>> da3ffeaf22b803cda544d0c1186b1fbae96d2f52
</div>